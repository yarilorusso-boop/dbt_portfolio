version: 2

sources:
  - name: jaffle_shop    # <--- Verifica che ci sia questo trattino seguito da NAME
    database: raw
    schema: jaffle_shop
    tables:
      - name: customers
      - name: orders
        config:
          freshness:
            warn_after: 
              count: 70000
              period:  hour
            error_after:
              count:  90000
              period:  hour
          loaded_at_field:  _etl_loaded_at
